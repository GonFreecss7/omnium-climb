// Bump this on every change that ships, so the deployed build can be
// identified on-device (shown at the bottom of the Session tab). See
// CLAUDE.md, "Version marker".
export const APP_VERSION = "2026-07-28.1";
